@extends('layouts.app')
@section('title', 'Kapten')
@section('content')
<div class="mb-5 flex items-start justify-between gap-3"><div><h1 class="text-2xl md:text-3xl font-black">Kapten</h1><p class="text-sm text-slate-500 mt-1">Data kapten untuk penugasan kapal.</p></div><a href="{{ route('captains.create') }}" class="rounded-2xl bg-teal-600 px-4 py-3 text-sm font-bold text-white">Tambah</a></div>
<form method="GET" data-allow-multi-submit="true" class="mb-4 flex gap-2 rounded-[1.5rem] bg-white p-3 shadow-sm border border-slate-100"><input name="search" value="{{ request('search') }}" placeholder="Cari kapten" class="min-h-[48px] flex-1 rounded-2xl border-slate-200 bg-slate-50 px-4 text-base"><button class="rounded-2xl bg-[#073b3a] px-4 text-sm font-bold text-white">Cari</button></form>
<div class="space-y-3 md:hidden">@forelse($captains as $captain)<div class="rounded-[1.5rem] bg-white p-4 shadow-sm border border-slate-100"><div class="flex justify-between gap-3"><div><div class="font-black text-lg">{{ $captain->name }}</div><div class="text-xs text-slate-500 mt-1">{{ $captain->phone ?: 'Nomor HP kosong' }}</div></div><span class="rounded-full px-3 py-1 text-xs font-bold h-fit {{ $captain->is_active ? 'bg-emerald-50 text-emerald-700' : 'bg-slate-100 text-slate-500' }}">{{ $captain->is_active ? 'Aktif' : 'Nonaktif' }}</span></div><a href="{{ route('captains.edit', $captain) }}" class="mt-3 inline-flex rounded-xl bg-teal-50 px-3 py-2 text-sm font-bold text-teal-700">Edit</a></div>@empty<div class="rounded-[1.5rem] bg-white p-8 text-center text-slate-500">Belum ada kapten.</div>@endforelse</div>
<div class="hidden md:block rounded-[1.5rem] bg-white shadow-sm border border-slate-100 overflow-hidden"><table class="min-w-full text-sm"><thead class="bg-slate-50"><tr><th class="p-4 text-left">Nama</th><th class="p-4 text-left">HP</th><th class="p-4 text-left">Keadaan</th><th class="p-4 text-right">Aksi</th></tr></thead><tbody>@forelse($captains as $captain)<tr class="border-t border-slate-100"><td class="p-4 font-bold">{{ $captain->name }}</td><td class="p-4">{{ $captain->phone }}</td><td class="p-4">{{ $captain->is_active ? 'Aktif' : 'Nonaktif' }}</td><td class="p-4 text-right"><a class="font-bold text-teal-700" href="{{ route('captains.edit', $captain) }}">Edit</a></td></tr>@empty<tr><td colspan="4" class="p-8 text-center text-slate-500">Belum ada kapten.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $captains->links() }}</div>
@endsection
