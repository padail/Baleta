@extends('layouts.app')
@section('title', 'Detail Tutup Bulan')
@section('content')
<div class="mb-5 flex flex-col md:flex-row md:items-start md:justify-between gap-3"><div><a href="{{ route('monthly-closings.index') }}" class="inline-flex text-sm font-semibold text-slate-500 mb-3">← Daftar tutup bulan</a><h1 class="text-2xl md:text-3xl font-black">{{ $closing->closing_number }}</h1><p class="text-sm text-slate-500 mt-1">Periode {{ str_pad($closing->month, 2, '0', STR_PAD_LEFT) }}/{{ $closing->year }} · Rekap final owner dari kapal</p></div><a href="{{ route('monthly-closings.print', $closing) }}" target="_blank" class="rounded-2xl bg-[#073b3a] px-4 py-3 text-center text-sm font-bold text-white">Cetak</a></div>
<div class="grid grid-cols-2 md:grid-cols-4 gap-3 mb-5"><div class="rounded-[1.5rem] bg-white p-4 shadow-sm border border-slate-100"><div class="text-xs text-slate-500">Bersih Harian</div><div class="text-lg font-black">{{ \App\Support\Money::rupiah($closing->daily_net_income) }}</div></div><div class="rounded-[1.5rem] bg-white p-4 shadow-sm border border-slate-100"><div class="text-xs text-slate-500">Operasional Kapal</div><div class="text-lg font-black text-amber-700">{{ \App\Support\Money::rupiah($closing->operational_expense_total) }}</div></div><div class="rounded-[1.5rem] bg-white p-4 shadow-sm border border-slate-100"><div class="text-xs text-slate-500">Jasa Kapten</div><div class="text-lg font-black">{{ \App\Support\Money::rupiah($closing->captain_share) }}</div></div><div class="rounded-[1.5rem] bg-[#073b3a] text-white p-4 shadow-sm col-span-2 md:col-span-1"><div class="text-xs text-slate-400">Owner Final</div><div class="text-xl font-black text-emerald-300">{{ \App\Support\Money::rupiah($closing->owner_share) }}</div></div></div>
<div class="rounded-[1.5rem] bg-amber-50 border border-amber-100 p-4 text-sm text-amber-900 mb-5">Pengeluaran non-operasional tidak masuk dan tidak ditampilkan pada tutup bulan ini. Rekap tersebut berdiri sendiri di menu <a href="{{ route('expenses.index') }}" class="font-black underline">Non-Operasional</a>.</div>
<div class="space-y-4 mb-5">@foreach($closing->shipItems as $shipItem)<section class="rounded-[1.75rem] bg-white shadow-sm border border-slate-100 overflow-hidden"><div class="p-4 md:p-5 bg-[#073b3a] text-white flex flex-col md:flex-row md:items-center md:justify-between gap-2"><div><h2 class="text-xl font-black">{{ $shipItem->ship_name }}</h2><p class="text-sm text-slate-300">Kapten: {{ $shipItem->captain_name }} · {{ $shipItem->total_invoices }} invoice</p></div><div class="md:text-right"><div class="text-xs text-slate-400">Owner dari kapal</div><div class="text-2xl font-black text-emerald-300">{{ \App\Support\Money::rupiah($shipItem->owner_share) }}</div></div></div><div class="p-4 grid grid-cols-2 md:grid-cols-5 gap-3 text-sm"><div class="rounded-2xl border p-3"><div class="text-xs text-slate-500">Bersih Harian</div><div class="font-black">{{ \App\Support\Money::rupiah($shipItem->total_daily_net_income) }}</div></div><div class="rounded-2xl border p-3"><div class="text-xs text-slate-500">Operasional</div><div class="font-black text-amber-700">{{ \App\Support\Money::rupiah($shipItem->total_ship_operational_expense) }}</div></div><div class="rounded-2xl border p-3"><div class="text-xs text-slate-500">Setelah Op.</div><div class="font-black text-teal-700">{{ \App\Support\Money::rupiah($shipItem->net_after_ship_operational) }}</div></div><div class="rounded-2xl border p-3"><div class="text-xs text-slate-500">Kapten {{ $shipItem->captain_percentage }}%</div><div class="font-black">{{ \App\Support\Money::rupiah($shipItem->captain_share) }}</div></div><div class="rounded-2xl border p-3 col-span-2 md:col-span-1"><div class="text-xs text-slate-500">Owner Kapal</div><div class="font-black text-green-700">{{ \App\Support\Money::rupiah($shipItem->owner_share) }}</div></div></div><div class="p-4 grid md:grid-cols-2 gap-4"><div class="rounded-2xl border border-slate-100 overflow-hidden"><div class="p-3 bg-slate-50 font-black text-sm">Invoice Harian</div><div class="divide-y divide-slate-100">@foreach($shipItem->invoiceItems as $invoiceItem)<div class="p-3 flex justify-between gap-3 text-sm"><div><div class="font-bold">{{ $invoiceItem->invoice_number }}</div><div class="text-xs text-slate-500">{{ $invoiceItem->invoice_date->format('d/m/Y') }} · {{ $invoiceItem->total_boxes }} gabus</div></div><div class="font-black whitespace-nowrap">{{ \App\Support\Money::rupiah($invoiceItem->net_income) }}</div></div>@endforeach</div></div><div class="rounded-2xl border border-slate-100 overflow-hidden"><div class="p-3 bg-amber-50 font-black text-sm">Biaya Operasional Kapal</div><div class="divide-y divide-slate-100">@forelse($shipItem->operationalExpenses as $expense)<div class="p-3 flex justify-between gap-3 text-sm"><div class="font-bold">{{ $expense->description }}</div><div class="font-black text-amber-700 whitespace-nowrap">{{ \App\Support\Money::rupiah($expense->amount) }}</div></div>@empty<div class="p-4 text-sm text-slate-500">Tidak ada biaya operasional kapal.</div>@endforelse</div></div></div></section>@endforeach</div>
<div class="rounded-[1.75rem] bg-white shadow-sm border border-slate-100 p-4 md:p-5"><h2 class="font-black mb-3">Ringkasan Final Owner</h2><div class="space-y-3 text-sm"><div class="flex justify-between gap-3"><span>Total bersih harian semua kapal</span><strong>{{ \App\Support\Money::rupiah($closing->daily_net_income) }}</strong></div><div class="flex justify-between gap-3 text-amber-700"><span>Total operasional kapal</span><strong>{{ \App\Support\Money::rupiah($closing->operational_expense_total) }}</strong></div><div class="flex justify-between gap-3"><span>Total jasa kapten</span><strong>{{ \App\Support\Money::rupiah($closing->captain_share) }}</strong></div><div class="border-t pt-3 flex justify-between gap-3 text-green-700 text-base"><span class="font-black">Pendapatan final owner dari kapal</span><strong>{{ \App\Support\Money::rupiah($closing->owner_share) }}</strong></div></div></div>
@endsection
